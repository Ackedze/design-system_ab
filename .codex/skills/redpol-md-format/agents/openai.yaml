interface:
  display_name: "DS AB Pattern Authoring"
  short_description: "Prepare DS AB pattern markdown and component agent rules"
  default_prompt: "Use $redpol-md-format to convert DS AB pattern sources into canonical pattern markdown, then distribute exact component rules into rules.json and concise guidance into agent-context.json."
