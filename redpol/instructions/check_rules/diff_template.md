ЗАДАЧА:
Сформируй финальный ответ пользователю на основе фактического diff между текстом до форматирования и итоговым текстом агента.

Порядок работы:

1. Извлеки итоговый текст из блока «ОТВЕТ АГЕНТА ФОРМАТИРОВАНИЯ».
2. Сравни «ТЕКСТ ДО ФОРМАТИРОВАНИЯ» и итоговый текст агента.
3. Оставь в причинах только реальные изменения.
4. Удали причины, где фрагмент ДО и ПОСЛЕ совпадает.
5. Не добавляй проверки без изменений.
6. Не редактируй итоговый текст агента.
7. Не исправляй текст дополнительно.
8. Не добавляй новых правил и новых причин, если они не подтверждаются фактическим изменением.

Формат ответа:

1. Итоговый текст:
   <итоговый текст агента целиком>

2. Причины изменений:

- <фрагмент до> → <фрагмент после> — <краткая причина>

Если итоговый текст полностью совпадает с текстом до форматирования:

1. Итоговый текст:
   <исходный текст без изменений>

2. Причины изменений:

- Нет изменений
